@model MarketApp.VarlikKatmani.Models.Kategori
@{
    ViewBag.Title = "Kategori Sil";
    Layout = "~/Areas/Yonetim/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card shadow-sm">
            <div class="card-header bg-danger text-white">
                <h2 class="mb-0">Kategori Sil</h2>
            </div>
            <div class="card-body">
                <p class="lead">Aşağıdaki kategoriyi silmek istediğinizden emin misiniz?</p>
                <dl class="row">
                    <dt class="col-md-4">Ad:</dt>
                    <dd class="col-md-8">@Model.Ad</dd>
                </dl>
                @using (Html.BeginForm("SilConfirmed", "Kategori", new { id = Model.Id, area = "Yonetim" }, FormMethod.Post, new { @class = "form-horizontal" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Id)
                    <div class="form-group row">
                        <div class="col-md-12 text-center">
                            <button type="submit" class="btn btn-danger">Sil</button>
                            <a href="@Url.Action("Listele", "Kategori", new { area = "Yonetim" })" class="btn btn-secondary">İptal</a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>